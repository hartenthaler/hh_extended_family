<?php

use Fisharebest\Webtrees\I18N;

/**
 * @var object $extfam_obj
 * @var object $filterObj
 * @var string $filterOption
 * @var string $propName
 * @var string $summaryName
 */

?>

                            <div id="hh-extended-family-summary">
                                <?php if ($propName == 'great_grandparents'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one great-grandmother recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one great-grandfather recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one great-grandparent of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandmother recorded.',  '%2$s has %1$d great-grandmothers recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandfather recorded.', '%2$s has %1$d great-grandfathers recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandparent of diverse or unknown sex recorded.', '%2$s has %1$d great-grandparents of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandfather and', '%2$s has %1$d great-grandfathers and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-grandmother recorded (%d in total).', '%d great-grandmothers recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandfather and', '%2$s has %1$d great-grandfathers and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-grandparent of diverse or unknown sex recorded (%d in total).', '%d great-grandparents of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandmother and', '%2$s has %1$d great-grandmothers and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-grandparent of diverse or unknown sex recorded (%d in total).', '%d great-grandparents of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandfather,', '%2$s has %1$d great-grandfathers,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-grandmother, and', '%d great-grandmothers, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d great-grandparent of diverse or unknown sex recorded (%d in total).', '%d great-grandparents of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'grandparents'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one grandmother recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one grandfather recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one grandparent of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandmother recorded.',  '%2$s has %1$d grandmothers recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandfather recorded.', '%2$s has %1$d grandfathers recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandparent of diverse or unknown sex recorded.', '%2$s has %1$d grandparents of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandfather and', '%2$s has %1$d grandfathers and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d grandmother recorded (%d in total).', '%d grandmothers recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandfather and', '%2$s has %1$d grandfathers and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d grandparent of diverse or unknown sex recorded (%d in total).', '%d grandparents of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandmother and', '%2$s has %1$d grandmothers and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d grandparent of diverse or unknown sex recorded (%d in total).', '%d grandparents of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandfather,', '%2$s has %1$d grandfathers,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d grandmother, and', '%d grandmothers, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d grandparent of diverse or unknown sex recorded (%d in total).', '%d grandparents of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'uncles_and_aunts'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one aunt recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one uncle recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one uncle or aunt of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d aunt recorded.',  '%2$s has %1$d aunts recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d uncle recorded.', '%2$s has %1$d uncles recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d uncle or aunt of diverse or unknown sex recorded.', '%2$s has %1$d uncles or aunts of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d uncle and', '%2$s has %1$d uncles and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d aunt recorded (%d in total).', '%d aunts recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d uncle and', '%2$s has %1$d uncles and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d uncle or aunt of diverse or unknown sex recorded (%d in total).', '%d uncles or aunts of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d aunt and', '%2$s has %1$d aunts and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d uncle or aunt of diverse or unknown sex recorded (%d in total).', '%d uncles or aunts of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d uncle,', '%2$s has %1$d uncles,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d aunt, and', '%d aunts, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d uncle or aunt of diverse or unknown sex recorded (%d in total).', '%d uncles or aunts of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'uncles_and_aunts_bm'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one aunt by marriage recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one uncle by marriage recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one uncle or aunt by marriage of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d aunt by marriage recorded.',  '%2$s has %1$d aunts by marriage recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d uncle by marriage recorded.', '%2$s has %1$d uncles by marriage recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d uncle or aunt by marriage of diverse or unknown sex recorded.', '%2$s has %1$d uncles or aunts by marriage of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d uncle by marriage and', '%2$s has %1$d uncles by marriage and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d aunt by marriage recorded (%d in total).', '%d aunts by marriage recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d uncle by marriage and', '%2$s has %1$d uncles by marriage and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d uncle or aunt by marriage of diverse or unknown sex recorded (%d in total).', '%d uncles or aunts by marriage of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d aunt by marriage and', '%2$s has %1$d aunts by marriage and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d uncle or aunt by marriage of diverse or unknown sex recorded (%d in total).', '%d uncles or aunts by marriage of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d uncle by marriage,', '%2$s has %1$d uncles by marriage,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d aunt by marriage, and', '%d aunts by marriage, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d uncle or aunt by marriage of diverse or unknown sex recorded (%d in total).', '%d uncles or aunts by marriage of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'parents'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one mother recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one father recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one parent of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d mother recorded.',  '%2$s has %1$d mothers recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d father recorded.', '%2$s has %1$d fathers recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d parent of diverse or unknown sex recorded.', '%2$s has %1$d parents of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d father and', '%2$s has %1$d fathers and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d mother recorded (%d in total).', '%d mothers recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d father and', '%2$s has %1$d fathers and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d parent of diverse or unknown sex recorded (%d in total).', '%d parents of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d mother and', '%2$s has %1$d mothers and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d parent of diverse or unknown sex recorded (%d in total).', '%d parents of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d father,', '%2$s has %1$d fathers,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d mother, and', '%d mothers, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d parent of diverse or unknown sex recorded (%d in total).', '%d parents of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                    <?php if ($extfam_obj->config->showLabels && $extfam_obj->proband->labels): ?>
                                        <?php foreach ($extfam_obj->proband->labels as $label): ?>
                                            <span class="hh_extended_family_label"><?= $label ?></span>&nbsp;
                                        <?php endforeach ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'parents_in_law'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one mother-in-law recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one father-in-law recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one parent-in-law of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d mother-in-law recorded.',  '%2$s has %1$d mothers-in-law recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d father-in-law recorded.', '%2$s has %1$d fathers-in-law recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d parent-in-law of diverse or unknown sex recorded.', '%2$s has %1$d parents-in-law of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d father-in-law and', '%2$s has %1$d fathers-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d mother-in-law recorded (%d in total).', '%d mothers-in-law recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d father-in-law and', '%2$s has %1$d fathers-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d parent-in-law of diverse or unknown sex recorded (%d in total).', '%d parents-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d mother-in-law and', '%2$s has %1$d mothers-in-law and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d parent-in-law of diverse or unknown sex recorded (%d in total).', '%d parents-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d father-in-law,', '%2$s has %1$d fathers-in-law,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d mother-in-law, and', '%d mothers-in-law, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d parent-in-law of diverse or unknown sex recorded (%d in total).', '%d parents-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'co_parents_in_law'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one co-mother-in-law recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one co-father-in-law recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one co-parent-in-law of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-mother-in-law recorded.',  '%2$s has %1$d co-mothers-in-law recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-father-in-law recorded.', '%2$s has %1$d co-fathers-in-law recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-parent-in-law of diverse or unknown sex recorded.', '%2$s has %1$d co-parents-in-law of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-father-in-law and', '%2$s has %1$d co-fathers-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d co-mother-in-law recorded (%d in total).', '%d co-mothers-in-law recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-father-in-law and', '%2$s has %1$d co-fathers-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d co-parent-in-law of diverse or unknown sex recorded (%d in total).', '%d co-parents-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-mother-in-law and', '%2$s has %1$d co-mothers-in-law and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d co-parent-in-law of diverse or unknown sex recorded (%d in total).', '%d co-parents-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-father-in-law,', '%2$s has %1$d co-fathers-in-law,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d co-mother-in-law, and', '%d co-mothers-in-law, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d co-parent-in-law of diverse or unknown sex recorded (%d in total).', '%d co-parents-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'partners'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one female partner recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one male partner recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one partner of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->popCount == 0): ?>
                                            <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                                <h3><?= I18N::plural('%2$s has %1$d female partner recorded.',  '%2$s has %1$d female partners recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                            <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                                <h3><?= I18N::plural('%2$s has %1$d male partner recorded.', '%2$s has %1$d male partners recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                            <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                                <h3><?= I18N::plural('%2$s has %1$d partner of diverse or unknown sex recorded.', '%2$s has %1$d partners of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                            <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                                <h3><?= I18N::plural('%2$s has %1$d male partner and', '%2$s has %1$d male partners and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                    <?= I18N::plural('%d female partner recorded (%d in total).', '%d female partners recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                            <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                                <h3><?= I18N::plural('%2$s has %1$d male partner and', '%2$s has %1$d male partners and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                    <?= I18N::plural('%d partner of diverse or unknown sex recorded (%d in total).', '%d partners of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                            <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                                <h3><?= I18N::plural('%2$s has %1$d female partner and', '%2$s has %1$d female partners and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                    <?= I18N::plural('%d partner of diverse or unknown sex recorded (%d in total).', '%d partners of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                            <?php else: ?>
                                                <h3><?= I18N::plural('%2$s has %1$d male partner,', '%2$s has %1$d male partners,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                    <?= I18N::plural('%d female partner, and', '%d female partners, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                    <?= I18N::plural('%d partner of diverse or unknown sex recorded (%d in total).', '%d partners of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                            <?php endif ?>
                                        <?php else: ?>
                                            <?php if ($extfam_obj->proband->indi->sex() == 'M'): ?>
                                                <h3><?= I18N::plural('%2$s has %1$d female partner and', '%2$s has %1$d female partners and', $filterObj->efp->$propName->pfemaleCount, $filterObj->efp->$propName->pfemaleCount, $summaryName) . ' ' ?>
                                                    <?= I18N::plural('%d male partner of female partners recorded (%d in total).', '%d male partners of female partners recorded (%d in total).', $filterObj->efp->$propName->popmaleCount, $filterObj->efp->$propName->popmaleCount, $filterObj->efp->$propName->allCount) ?>
                                                </h3>
                                            <?php elseif ($extfam_obj->proband->indi->sex() == 'F'): ?>
                                                <h3><?= I18N::plural('%2$s has %1$d male partner and', '%2$s has %1$d male partners and', $filterObj->efp->$propName->pmaleCount, $filterObj->efp->$propName->pmaleCount, $summaryName) . ' ' ?>
                                                    <?= I18N::plural('%d female partner of male partners recorded (%d in total).', '%d female partners of male partners recorded (%d in total).', $filterObj->efp->$propName->popfemaleCount, $filterObj->efp->$propName->popfemaleCount, $filterObj->efp->$propName->allCount) ?>
                                                </h3>
                                            <?php else: ?>
                                                <h3><?= I18N::plural('%2$s has %1$d partner and', '%2$s has %1$d partners and', $filterObj->efp->$propName->pCount, $filterObj->efp->$propName->pCount, $summaryName) . ' ' ?>
                                                    <?= I18N::plural('%1$d partner of partners recorded (%2$d in total).', '%1$d partners of partners recorded (%2$d in total).', $filterObj->efp->$propName->popCount, $filterObj->efp->$propName->popCount, $filterObj->efp->$propName->allCount) ?>
                                                </h3>
                                            <?php endif ?>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'partner_chains'): ?>
                                    <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                        <h3><?php echo(I18N::plural('%d female partner in this partner chain recorded', '%d female partners in this partner chain recorded', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) .
                                                (($extfam_obj->proband->indi->sex() == 'F')? ' ('.I18N::translate('including %s', $summaryName).')':'') . '.');
                                        ?></h3>
                                    <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                        <h3><?php echo(I18N::plural('%d male partner in this partner chain recorded', '%d male partners in this partner chain recorded', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount) .
                                                (($extfam_obj->proband->indi->sex() == 'M')? ' ('.I18N::translate('including %s', $summaryName).')':'') . '.');
                                        ?></h3>
                                    <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                        <h3><?php echo(I18N::plural('%d partner of diverse or unknown sex in this partner chain recorded', '%d partners of diverse or unknown sex in this partner chain recorded', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount) .
                                                (($extfam_obj->proband->indi->sex() !== 'M' && $extfam_obj->proband->indi->sex() !== 'F')? ' ('.I18N::translate('including %s', $summaryName).')':'') . '.');
                                        ?></h3>
                                    <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                        <h3><?= I18N::plural('%2$s has %1$d male partner and', '%2$s has %1$d male partners and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                            <?php echo(I18N::plural('%1$d female partner in this partner chain recorded (%2$d in total', '%1$d female partners in this partner chain recorded (%2$d in total', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) .
                                                (($extfam_obj->proband->indi->sex() === 'M' || $extfam_obj->proband->indi->sex() === 'F')? ', '.I18N::translate('including %s', $summaryName).')':')') . '.');
                                        ?></h3>
                                    <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                        <h3><?= I18N::plural('%2$s has %1$d male partner and', '%2$s has %1$d male partners and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                            <?php echo(I18N::plural('%1$d partner of diverse or unknown sex in this partner chain recorded (%2$d in total', '%1$d partners of diverse or unknown sex in this partner chain recorded (%2$d in total', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) .
                                                (($extfam_obj->proband->indi->sex() !== 'F')? ', '.I18N::translate('including %s', $summaryName).')':')') . '.');
                                        ?></h3>
                                    <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                        <h3><?= I18N::plural('%2$s has %1$d female partner and', '%2$s has %1$d female partners and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                            <?php echo(I18N::plural('%1$d partner of diverse or unknown sex in this partner chain recorded (%2$d in total', '%1$d partners of diverse or unknown sex in this partner chain recorded (%2$d in total', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) .
                                                (($extfam_obj->proband->indi->sex() !== 'M')? ', '.I18N::translate('including %s', $summaryName).')':')') . '.');
                                        ?></h3>
                                    <?php else: ?>
                                        <h3><?= I18N::plural('%2$s has %1$d male partner and', '%2$s has %1$d male partners and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                            <?= I18N::plural('%d female partner, and', '%d female partners, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                            <?= I18N::plural('%1$d partner of diverse or unknown sex in this partner chain recorded (%2$d in total, including %3$s).', '%1$d partners of diverse or unknown sex in this partner chain recorded (%2$d in total, including %3$s).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount, $summaryName) ?>
                                        </h3>
                                    <?php endif ?>
                                    <?php if ($filterObj->efp->$propName->chains->chainsCount > 1): ?>
                                        <h3><?= I18N::translate('There are %d branches in the partner chain.', $filterObj->efp->$propName->chains->chainsCount, $filterObj->efp->$propName->chains->chainsCount) . ' ' ?>
                                            <?php if ($filterOption == 'all'): ?>
                                                <?= I18N::translate('The longest branch in the partner chain to %2$s consists of %1$d partners (including %3$s).', $filterObj->efp->$propName->chains->longestChainCount, $filterObj->efp->$propName->chains->mostDistantPartner->fullname(), $summaryName) ?>
                                            <?php else: ?>
                                                <?= I18N::translate('The longest branch in the partner chain consists of %1$d partners (including %2$s).', $filterObj->efp->$propName->chains->longestChainCount, $summaryName) ?>
                                            <?php endif ?>
                                        </h3>
                                    <?php endif ?>
                                <?php elseif ($propName == 'siblings'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one sister recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one brother recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one sibling of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d sister recorded.',  '%2$s has %1$d sisters recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d brother recorded.', '%2$s has %1$d brothers recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d sibling of diverse or unknown sex recorded.', '%2$s has %1$d siblings of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d brother and', '%2$s has %1$d brothers and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d sister recorded (%d in total).', '%d sisters recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d brother and', '%2$s has %1$d brothers and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d sibling of diverse or unknown sex recorded (%d in total).', '%d siblings of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d sister and', '%2$s has %1$d sisters and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d sibling of diverse or unknown sex recorded (%d in total).', '%d siblings of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d brother,', '%2$s has %1$d brothers,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d sister, and', '%d sisters, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d sibling of diverse or unknown sex recorded (%d in total).', '%d siblings of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                    <?php if ($extfam_obj->config->showLabels && $extfam_obj->proband->labels): ?>
                                        <?php foreach ($extfam_obj->proband->labels as $label): ?>
                                            <span class="hh_extended_family_label"><?= $label ?></span>&nbsp;
                                        <?php endforeach ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'siblings_in_law'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one sister-in-law recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one brother-in-law recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one sibling-in-law of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d sister-in-law recorded.',  '%2$s has %1$d sisters-in-law recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d brother-in-law recorded.', '%2$s has %1$d brothers-in-law recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d sibling-in-law of diverse or unknown sex recorded.', '%2$s has %1$d siblings-in-law of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d brother-in-law and', '%2$s has %1$d brothers-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d sister-in-law recorded (%d in total).', '%d sisters-in-law recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d brother-in-law and', '%2$s has %1$d brothers-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d sibling-in-law of diverse or unknown sex recorded (%d in total).', '%d siblings-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d sister-in-law and', '%2$s has %1$d sisters-in-law and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d sibling-in-law of diverse or unknown sex recorded (%d in total).', '%d siblings-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d brother-in-law,', '%2$s has %1$d brothers-in-law,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d sister-in-law, and', '%d sisters-in-law, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d sibling-in-law of diverse or unknown sex recorded (%d in total).', '%d siblings-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                    <?php if ($extfam_obj->config->showLabels && $extfam_obj->proband->labels): ?>
                                        <?php foreach ($extfam_obj->proband->labels as $label): ?>
                                            <span class="hh_extended_family_label"><?= $label ?></span>&nbsp;
                                        <?php endforeach ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'co_siblings_in_law'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one co-sister-in-law recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one co-brother-in-law recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one co-sibling-in-law of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-sister-in-law recorded.',  '%2$s has %1$d co-sisters-in-law recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-brother-in-law recorded.', '%2$s has %1$d co-brothers-in-law recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-sibling-in-law of diverse or unknown sex recorded.', '%2$s has %1$d co-siblings-in-law of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-brother-in-law and', '%2$s has %1$d co-brothers-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d co-sister-in-law recorded (%d in total).', '%d co-sisters-in-law recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-brother-in-law and', '%2$s has %1$d co-brothers-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d co-sibling-in-law of diverse or unknown sex recorded (%d in total).', '%d co-siblings-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-sister-in-law and', '%2$s has %1$d co-sisters-in-law and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d co-sibling-in-law of diverse or unknown sex recorded (%d in total).', '%d co-siblings-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d co-brother-in-law,', '%2$s has %1$d co-brothers-in-law,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d co-sister-in-law, and', '%d co-sisters-in-law, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d co-sibling-in-law of diverse or unknown sex recorded (%d in total).', '%d co-siblings-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                    <?php if ($extfam_obj->config->showLabels && $extfam_obj->proband->labels): ?>
                                        <?php foreach ($extfam_obj->proband->labels as $label): ?>
                                            <span class="hh_extended_family_label"><?= $label ?></span>&nbsp;
                                        <?php endforeach ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'cousins'): ?>								
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one female first cousin recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one male first cousin recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one first cousin of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d female first cousin recorded.',  '%2$s has %1$d female first cousins recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d male first cousin recorded.', '%2$s has %1$d male first cousins recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d first cousin of diverse or unknown sex recorded.', '%2$s has %1$d first cousins of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d male first cousin and', '%2$s has %1$d male first cousins and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d female first cousin recorded (%d in total).', '%d female first cousins recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d male first cousin and', '%2$s has %1$d male first cousins and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d first cousin of diverse or unknown sex recorded (%d in total).', '%d first cousins of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d female first cousin and', '%2$s has %1$d female first cousins and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d first cousin of diverse or unknown sex recorded (%d in total).', '%d first cousins of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d male first cousin,', '%2$s has %1$d male first cousins,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d female first cousin, and', '%d female first cousins, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d first cousin of diverse or unknown sex recorded (%d in total).', '%d first cousins of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'nephews_and_nieces'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one niece recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one nephew recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one nephew or niece of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d niece recorded.',  '%2$s has %1$d nieces recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d nephew recorded.', '%2$s has %1$d nephews recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d nephew or niece of diverse or unknown sex recorded.', '%2$s has %1$d nephews or nieces of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d nephew and', '%2$s has %1$d nephews and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d niece recorded (%d in total).', '%d nieces recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d nephew and', '%2$s has %1$d nephews and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d nephew or niece of diverse or unknown sex recorded (%d in total).', '%d nephews or nieces of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d niece and', '%2$s has %1$d nieces and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d nephew or niece of diverse or unknown sex recorded (%d in total).', '%d nephews or nieces of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d nephew,', '%2$s has %1$d nephews,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d niece, and', '%d nieces, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d nephew or niece of diverse or unknown sex recorded (%d in total).', '%d nephews or nieces of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'children'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one daughter recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one son recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one child of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d daughter recorded.',  '%2$s has %1$d daughters recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d son recorded.', '%2$s has %1$d sons recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d child of diverse or unknown sex recorded.', '%2$s has %1$d children of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d son and', '%2$s has %1$d sons and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d daughter recorded (%d in total).', '%d daughters recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d son and', '%2$s has %1$d sons and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d child of diverse or unknown sex recorded (%d in total).', '%d children of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d daughter and', '%2$s has %1$d daughters and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d child of diverse or unknown sex recorded (%d in total).', '%d children of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d son,', '%2$s has %1$d sons,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d daughter, and', '%d daughters, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d child of diverse or unknown sex recorded (%d in total).', '%d children of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'children_in_law'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one daughter-in-law recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one son-in-law recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one child-in-law of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d daughter-in-law recorded.',  '%2$s has %1$d daughters-in-law recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d son-in-law recorded.', '%2$s has %1$d sons-in-law recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d child-in-law of diverse or unknown sex recorded.', '%2$s has %1$d children-in-law of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d son-in-law and', '%2$s has %1$d sons-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d daughter-in-law recorded (%d in total).', '%d daughters-in-law recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d son-in-law and', '%2$s has %1$d sons-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d child-in-law of diverse or unknown sex recorded (%d in total).', '%d children-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d daughter-in-law and', '%2$s has %1$d daughters-in-law and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d child-in-law of diverse or unknown sex recorded (%d in total).', '%d children-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d son-in-law,', '%2$s has %1$d sons-in-law,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d daughter-in-law, and', '%d daughters-in-law, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d child-in-law of diverse or unknown sex recorded (%d in total).', '%d children-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'grandchildren'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one granddaughter recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one grandson recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one grandchild of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d granddaughter recorded.',  '%2$s has %1$d granddaughters recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandson recorded.', '%2$s has %1$d grandsons recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandchild of diverse or unknown sex recorded.', '%2$s has %1$d grandchildren of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandson and', '%2$s has %1$d grandsons and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d granddaughter recorded (%d in total).', '%d granddaughters recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandson and', '%2$s has %1$d grandsons and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d grandchild of diverse or unknown sex recorded (%d in total).', '%d grandchildren of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d granddaughter and', '%2$s has %1$d granddaughters and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d grandchild of diverse or unknown sex recorded (%d in total).', '%d grandchildren of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandson,', '%2$s has %1$d grandsons,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d granddaughter, and', '%d granddaughters, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d grandchild of diverse or unknown sex recorded (%d in total).', '%d grandchildren of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'great_grandchildren'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one great-granddaughter recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one great-grandson recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one great-grandchild of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-granddaughter recorded.',  '%2$s has %1$d great-granddaughters recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandson recorded.', '%2$s has %1$d great-grandsons recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandchild of diverse or unknown sex recorded.', '%2$s has %1$d great-grandchildren of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandson and', '%2$s has %1$d great-grandsons and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-granddaughter recorded (%d in total).', '%d great-granddaughters recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandson and', '%2$s has %1$d great-grandsons and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-grandchild of diverse or unknown sex recorded (%d in total).', '%d great-grandchildren of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-granddaughter and', '%2$s has %1$d great-granddaughters and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-grandchild of diverse or unknown sex recorded (%d in total).', '%d great-grandchildren of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandson,', '%2$s has %1$d great-grandsons,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-granddaughter, and', '%d great-granddaughters, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d great-grandchild of diverse or unknown sex recorded (%d in total).', '%d great-grandchildren of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'grandchildren_in_law'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one granddaughter-in-law recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one grandson-in-law recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one grandchild-in-law of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d granddaughter-in-law recorded.',  '%2$s has %1$d granddaughters-in-law recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandson-in-law recorded.', '%2$s has %1$d grandsons-in-law recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandchild-in-law of diverse or unknown sex recorded.', '%2$s has %1$d grandchildren-in-law of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandson-in-law and', '%2$s has %1$d grandsons-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d granddaughter-in-law recorded (%d in total).', '%d granddaughters-in-law recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandson-in-law and', '%2$s has %1$d grandsons-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d grandchild-in-law of diverse or unknown sex recorded (%d in total).', '%d grandchildren-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d granddaughter-in-law and', '%2$s has %1$d granddaughters-in-law and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d grandchild-in-law of diverse or unknown sex recorded (%d in total).', '%d grandchildren-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d grandson-in-law,', '%2$s has %1$d grandsons-in-law,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d granddaughter-in-law, and', '%d granddaughters-in-law, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                            <?= I18N::plural('%d grandchild-in-law of diverse or unknown sex recorded (%d in total).', '%d grandchildren-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php elseif ($propName == 'great_grandchild_in_law'): ?>
                                    <?php if ($filterObj->efp->$propName->allCount == 1): ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one great-granddaughter-in-law recorded.', $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 1): ?>
                                            <h3><?= I18N::translate('%s has one great-grandson-in-law recorded.', $summaryName) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::translate('%s has one great-grandchild-in-law of diverse or unknown sex recorded.', $summaryName) ?></h3>
                                        <?php endif ?>
                                    <?php else: ?>
                                        <?php if ($filterObj->efp->$propName->femaleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-granddaughter-in-law recorded.',  '%2$s has %1$d great-granddaughters-in-law recorded.', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandson-in-law recorded.', '%2$s has %1$d great-grandsons-in-law recorded.', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == $filterObj->efp->$propName->allCount): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandchild-in-law of diverse or unknown sex recorded.', '%2$s has %1$d great-grandchildren-in-law of diverse or unknown sex recorded.', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $summaryName) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->otherSexCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandson-in-law and', '%2$s has %1$d great-grandsons-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-granddaughter-in-law recorded (%d in total).', '%d great-granddaughters-in-law recorded (%d in total).', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->femaleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandson-in-law and', '%2$s has %1$d great-grandsons-in-law and', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-grandchild-in-law of diverse or unknown sex recorded (%d in total).', '%d great-grandchildren-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php elseif ($filterObj->efp->$propName->maleCount == 0): ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-granddaughter-in-law and', '%2$s has %1$d great-granddaughters-in-law and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-grandchild-in-law of diverse or unknown sex recorded (%d in total).', '%d great-grandchildren-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php else: ?>
                                            <h3><?= I18N::plural('%2$s has %1$d great-grandson-in-law,', '%2$s has %1$d great-grandsons-in-law,', $filterObj->efp->$propName->maleCount, $filterObj->efp->$propName->maleCount, $summaryName) . ' ' ?>
                                                <?= I18N::plural('%d great-granddaughter-in-law, and', '%d great-granddaughters-in-law, and', $filterObj->efp->$propName->femaleCount, $filterObj->efp->$propName->femaleCount) . ' ' ?>
                                                <?= I18N::plural('%d great-grandchild-in-law of diverse or unknown sex recorded (%d in total).', '%d great-grandchildren-in-law of diverse or unknown sex recorded (%d in total).', $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->otherSexCount, $filterObj->efp->$propName->allCount) ?></h3>
                                        <?php endif ?>
                                    <?php endif ?>
                                <?php endif ?>
                            </div>
